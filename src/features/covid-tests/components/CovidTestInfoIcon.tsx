import InfoCircle from '@assets/icons/InfoCircle';
import { colors } from '@theme';
import * as React from 'react';

export default function CovidTestInfoIcon() {
  return <InfoCircle color={colors.primary} height={15} width={15} />;
}
